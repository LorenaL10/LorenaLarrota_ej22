#include <iostream>
#include <cstdlib>
#include <ctime>

using std::cout;
using std::endl;

void metropolis(int N=1000);

int main(){
    float f(float x){
        return exp(x*x/2)
    }
    int N;
    metropolis (N)
    cout << dato << " " << intento << endl;
}
return 0
}
    
    
void metropolis(int N=1000)
{
for(i=0;i<N;i++){ 
   srand48(5);
   float dato;  
   float propuesta  = drand48() + drand48()
   float alpha = drand48()
        if(alpha<propuesta){
            dato+=propuesta;
        }
        else{
            dato+=alpha
        }
    *dato=dato;
    *intento=i;
    }
  }
