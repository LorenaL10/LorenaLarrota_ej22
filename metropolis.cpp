#include <iostream>
#include <cstdlib>
#include <ctime>

using std::cout;
using std::endl;

void metropolis();

int main(){
    int N=1000;
    int f=exp(-x*x/2);
    srand48(5);
    
    
void metropolis{
for(i=0;i<N;i++){ 
    
        float propuesta  = drand48() + drand48()
        float alpha = drand48()
        if(alpha<propuesta):
            lista.append(propuesta)
        else:
            lista.append(lista[i-1])
    return np.array(lista)
    }
  }
